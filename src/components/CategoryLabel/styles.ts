import styled from "styled-components";

export const CategoryLabelContainer = styled.div`
  background: #1abdb3;
  font-weight: lighter;
  border-radius: 4px;
  display: block;
  padding: 4px 8px;
  font-size: 1rem;
  position: relative;
  z-index: 2;

  a {
    color: #fff;
  }
`;
